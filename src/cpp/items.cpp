#include "items.h"

Items::Items(int head, int neck, int shoulders, int back, int chest, int bracers, int gloves, int belt, int legs, int boots, int ring1, int ring2, int trinket1, int trinket2, int mainHand, int offHand, int twoHand, int wand)
    : head(head), neck(neck), shoulders(shoulders), back(back), chest(chest), bracers(bracers), gloves(gloves), belt(belt), legs(legs), boots(boots), ring1(ring1), ring2(ring2), trinket1(trinket1), trinket2(trinket2), mainHand(mainHand), offHand(offHand), twoHand(twoHand), wand(wand) {}